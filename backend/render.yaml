# Configuração para deploy no Render.com
services:
  - type: web
    name: compound-interest-api
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8080
